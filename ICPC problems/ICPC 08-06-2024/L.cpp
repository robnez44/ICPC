#include <bits/stdc++.h>
using namespace std;

int main(){

    int N;
    vector<int> a(N);
    vector<int> b(N);

    while (cin >> N)
    {
        for (int i = 0; i < ; ++i)
        {
            
        }
        
    }
    


    return 0;
}